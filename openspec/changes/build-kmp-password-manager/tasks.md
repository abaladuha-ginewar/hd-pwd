## 1. 工程与质量基线

- [x] 1.1 初始化 Kotlin Multiplatform Gradle 工程、版本目录和 Android、Windows Desktop、Web 三个平台入口
- [x] 1.2 建立 domain、application、crypto、storage、sync、shared-ui 的共享模块或对应源码集边界
- [x] 1.3 配置 Compose Multiplatform、Coroutines、Serialization、Ktor、测试框架及统一代码格式检查
- [x] 1.4 使用 Docker Compose 建立三端编译和共享测试的 CI 流程，并验证最小应用均可启动
- [x] 1.5 配置生产日志脱敏入口和禁止敏感领域对象默认输出的编码约束

## 2. 密码学依赖与基础协议

- [x] 2.1 调研并验证三端 Argon2id、XChaCha20-Poly1305、HKDF/HMAC、安全随机数和确定性 CBOR 依赖的维护状态、许可和测试向量
- [x] 2.2 通过最低支持设备基准确定本机包装、Vault 数据和 V1 生成 KDF 参数，并记录供应链固定方案
- [x] 2.3 实现可覆盖敏感字节容器、作用域清理工具和禁止隐式字符串化的密钥类型
- [x] 2.4 定义密码学门面及 Android/JVM/Web 实现，覆盖随机数、Argon2id、HKDF、HMAC 和 AEAD
- [x] 2.5 实现带 magic、版本、KDF/AEAD 参数、盐、nonce 和认证头部的通用加密容器编解码
- [x] 2.6 为用途派生定义稳定域标识并验证 DataKey、SyncKey、BackupKey 和 GeneratorKey 相互隔离
- [x] 2.7 添加错误密码、密文篡改、未知算法、nonce 和敏感缓冲区清理测试

## 3. V1 密码生成与恢复配方

- [x] 3.1 定义版本化 PasswordPolicy 模型、默认长度 20、默认符号集合及规则可满足性校验
- [x] 3.2 实现 `[A-Za-z_.-]+`、1 至 128 字符、区分大小写的 key 校验器
- [x] 3.3 实现 V1 固定参数 GeneratorKey 派生和基于 HMAC 的确定性计数器字节流
- [x] 3.4 实现无偏拒绝采样、字符类最少数量填充和确定性 Fisher-Yates 洗牌
- [x] 3.5 实现恢复配方的规范化 CBOR、Base64URL 外层格式、版本前缀和校验码
- [x] 3.6 生成并固化包含中间编码和最终密码的公开 V1 测试向量
- [ ] 3.7 在 Android、Windows 和 Web 测试中验证相同输入逐字节输出一致且 V1 不受升级影响

## 4. Vault 领域模型与用例

- [x] 4.1 定义 Vault、Folder、Entry、Label、SyncTarget、Tombstone、Conflict 及不可变 UUID 类型
- [x] 4.2 实现用户内 key 唯一性、三级目录、无环父子关系和固定/自定义 RGB 颜色约束
- [x] 4.3 实现密码项和文件夹的创建、编辑、移动、删除及文件夹级联事务用例
- [x] 4.4 实现 key 或密码规则变更的密码变化警告和恢复配方失效状态
- [x] 4.5 实现从当前目录递归按 key、标题、标签名、标签值进行不区分大小写包含搜索
- [x] 4.6 实现文件夹优先稳定排序和带相对目录路径的扁平搜索结果
- [x] 4.7 为领域约束、移动、级联删除、取消编辑和搜索范围添加共享单元测试

## 5. 本地用户、封装与授权会话

- [x] 5.1 实现最小明文用户索引、设备安装范围用户名唯一性和随机 Vault 存储定位
- [x] 5.2 实现用户创建时 LEK 生成、恢复密码 AEAD 加密和本机主密码 Argon2id 封装
- [x] 5.3 实现本机主密码验证、LEK 解封装和错误验证后的临时数据清理
- [x] 5.4 实现五分钟绝对授权状态机和限定用途、引用计数式操作许可
- [x] 5.5 实现会话到期、新操作拒绝、已有操作完成及最终敏感状态清理
- [x] 5.6 实现通过恢复密码验证 Vault 后重置本机主密码和重建本机封装
- [x] 5.7 实现二次确认删除本地用户，同时确保不向 S3 发送删除远端数据请求
- [x] 5.8 为多用户隔离、验证取消、会话边界和操作许可竞态添加测试

## 6. 生物识别平台适配

- [x] 6.1 定义生物识别能力检测、LEK 封装、解封装、失效和取消的共享接口
- [x] 6.2 实现 Android BiometricPrompt 与 Keystore 的硬件保护封装及主密码回退
- [ ] 6.3 实现 Windows 可用安全密钥/用户验证能力的封装及主密码回退
- [x] 6.4 验证 Web 可用硬件封装能力，仅在满足稳定解封装时启用，否则隐藏生物识别选项
- [x] 6.5 实现创建用户时的初始生物识别询问、取消默认值和权限/环境变化后的失效处理

## 7. 加密本地存储

- [x] 7.1 实现 VaultState 序列化、版本化认证加密和恢复密码临时派生 DataKey 的保存/读取用例
- [x] 7.2 实现 Android 应用私有文件的临时写入、认证校验、原子替换和异常恢复
- [x] 7.3 实现 Windows 应用私有文件的临时写入、认证校验、原子替换和异常恢复
- [ ] 7.4 实现 Web IndexedDB 的用户索引、加密 Blob、dirty 状态和事务化提交
- [x] 7.5 实现每次业务变更后的实时本地保存队列、代次合并和失败提示
- [x] 7.6 实现锁定、返回用户列表和删除用户时解密 Vault、搜索索引及敏感缓冲区清理
- [x] 7.7 添加进程中断、损坏临时文件、IndexedDB 事务失败和最后成功版本恢复测试

## 8. 备份导入导出与恢复迁移

- [x] 8.1 实现包含 VaultState 和同步元数据、排除用户名及本机封装的版本化 `.dat` 备份容器
- [x] 8.2 实现用户名安全转义和 `<用户名>_<年>-<月>-<日>_<时>-<分>-<秒>.dat` 导出命名
- [x] 8.3 实现备份导出的操作许可、临时 BackupKey 派生和完成后清理
- [x] 8.4 实现临时区域中的备份格式、AEAD、恢复密码、key 唯一性和目录约束验证
- [x] 8.5 实现验证全部成功后原子创建新用户名及本机封装，失败时不留下半成品用户
- [x] 8.6 实现导入 S3 配置的待确认状态和用户确认前禁止网络请求
- [x] 8.7 实现无 Vault 恢复页面，通过恢复密码与恢复配方或手工规则生成子密码
- [x] 8.8 实现恢复密码全库迁移的本地新 epoch、两阶段提交、失败回滚和历史备份警告
- [x] 8.9 添加重命名备份、错误密码、损坏文件、旧格式兼容和迁移回滚测试

## 9. 共享 UI 与敏感交互

- [ ] 9.1 实现用户列表、创建/导入用户、本机主密码验证、生物识别回退和到期验证页面
- [x] 9.2 实现密码库顶部返回/用户名/删除、搜索栏和设置/导出/添加竖向悬浮按钮
- [x] 9.3 实现文件夹优先的响应式网格、窄屏单列和宽屏多列布局
- [x] 9.4 实现密码项卡片、标签逐行展示、标签值复制及长按编辑交互
- [x] 9.5 实现密码项与文件夹添加/编辑/删除确认、三级目录限制、移动和颜色选择器
- [x] 9.6 实现当前目录递归搜索结果、相对路径展示和文件夹层级导航
- [x] 9.7 实现恢复配方复制、格式校验、输入变化警告和无 Vault 恢复交互
- [x] 9.8 实现密码按需生成、默认掩码、显示一分钟自动隐藏及后台立即隐藏
- [x] 9.9 实现密码和恢复配方复制十五秒后的条件剪贴板清除，避免覆盖后来内容
- [x] 9.10 添加可测试时钟驱动的显示、剪贴板、授权到期和响应式布局 UI 测试

## 10. S3 客户端与目标配置

- [x] 10.1 定义 S3 目标、提供商预设、独立状态、水位、凭据和待确认模型
- [x] 10.2 实现通用 S3 Signature V4、TLS 校验、列举、读取、条件写入和上传基础客户端
- [x] 10.3 建立通用 S3、阿里云、腾讯云、七牛云的 endpoint、region、path-style 和 CORS 测试矩阵
- [x] 10.4 实现配置编辑、连接测试、凭据临时解密、脱敏错误和加密保存
- [x] 10.5 实现每个目标的空闲、待同步、同步中、成功、失败状态及设置红点和顶部提示
- [x] 10.6 验证所有 S3 请求、日志和对象路径不包含用户名、key、标题、标签或 Secret
- [x] 10.7 使用 Docker 启动 Silo 测试服务，创建三个 bucket 并验证多个 S3 配置的连接与隔离

## 11. 增量、合并与多副本传播

- [x] 11.1 定义 DeviceId、EventId、设备序号、因果版本、HLC、字段变更和事务标识协议
- [x] 11.2 实现本地新增、修改、移动、删除到加密 delta 包的事件生成
- [x] 11.3 实现五秒静默调度、计时重置、本地保存前置条件和同步中再修改的代次处理
- [x] 11.4 实现单目标 head、delta、snapshot 对象布局及未知事件拉取和 EventId 去重
- [x] 11.5 实现因果更新覆盖、并发不同字段合并和同字段确定性选值及候选保留
- [x] 11.6 实现删除优先墓碑、文件夹级联事务和离线设备防复活规则
- [x] 11.7 实现 key 唯一性、目录环和超三级同步冲突的检测及人工解决用例
- [x] 11.8 实现从全部启用目标拉取、按独立水位合并并向其他所有目标传播缺失事件
- [x] 11.9 实现指数退避与抖动重试、部分目标失败隔离和重复传播环路防护
- [x] 11.10 实现包含状态、冲突、墓碑和设备水位的快照生成、加载及安全增量压缩
- [x] 11.11 实现远端新 S3 配置待确认提示，确认后才加入自动连接和传播集合
- [x] 11.12 添加多设备并发字段修改、key 冲突、级联删除、离线回归和三副本最终收敛测试

## 12. 平台生命周期、发布验证与安全检查

- [x] 12.1 实现 Windows 未同步关闭提示、等待同步完成后自动关闭和明确取消等待
- [x] 12.2 实现 Android 后台 dirty 持久化、尽力同步和下次启动继续同步
- [ ] 12.3 实现 Web Page Visibility、IndexedDB dirty 持久化和 `beforeunload` 尽力提示
- [x] 12.4 验证生产构建的日志、崩溃报告和网络流量不泄露规格定义的敏感数据
- [ ] 12.5 执行跨平台密码学测试向量、备份互操作、同步故障注入和存储崩溃恢复测试
- [x] 12.6 执行依赖漏洞、许可、供应链锁定和平台安全配置检查并处理阻断项
- [x] 12.7 在 Docker 构建环境中构建并烟测 Android 安装包、Windows 应用和 Web 产物，记录平台能力降级行为
