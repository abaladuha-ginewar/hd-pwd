## 1. 设备锁模型与存储

- [x] 1.1 定义设备锁记录（世代、主密码包装的 DeviceLEK、preferBiometric）及与用户索引分离的读写接口
- [x] 1.2 将每用户恢复密码封装改为由 DeviceLEK 加密，并写入 deviceGeneration 与用户 id 附加认证数据
- [x] 1.3 调整 LocalAppRepository：设备锁、设备级生物识别封装、每用户恢复密码封装分键存储，删除用户不删设备锁
- [x] 1.4 为设备锁读写、世代不匹配和用户索引不含密钥材料添加测试

## 2. 封装、验证与授权会话

- [x] 2.1 将 LocalEnvelopeService 从「每用户 LEK + 主密码」改为「DeviceLEK 包装 + 按用户封装恢复密码」
- [x] 2.2 实现首次设置、主密码解开、生物识别解开、修改主密码（不换世代）和忘记主密码（换世代并只重绑当前用户）
- [x] 2.3 实现待重绑检测：世代不一致时拒绝用 DeviceLEK 解密，必须用该用户恢复密码重绑
- [x] 2.4 调整 AuthorizationSession：缓存 DeviceLEK；返回用户列表不单独清会话；到期清 DeviceLEK
- [x] 2.5 为操作许可补充创建用户与设备设置用途，并保持已获许可操作可在到期后做完
- [x] 2.6 添加错误主密码、生物识别回退、改密不伤用户封装、重置后其他用户待重绑的测试

## 3. 生物识别设备级适配

- [x] 3.1 将生物识别封装 label 从用户 id 改为设备级标识，并保留能力检测与主密码回退
- [x] 3.2 实现 preferBiometric 与「当前是否存在有效封装」分离：开启需初始验证，关闭删除封装但不换 DeviceLEK
- [x] 3.3 确保 Web 与无硬件环境隐藏开关且验证仅走主密码
- [x] 3.4 更新生物识别注册/失效测试，覆盖默认偏好与手动切主密码

## 4. 创建导入、迁移与删除

- [x] 4.1 创建/导入用户改为先要求有效会话，表单不再收集本机主密码或生物识别勾选
- [x] 4.2 导入成功后用当前 DeviceLEK 封装恢复密码；失败仍不留半成品用户
- [x] 4.3 恢复密码全库迁移成功后把新恢复密码封装到现有 DeviceLEK，不轮换设备锁世代
- [x] 4.4 实现旧每用户主密码数据的启动迁移：设置新设备锁，用旧主密码或恢复密码逐用户重绑
- [x] 4.5 删除本地用户只清除该用户 Vault 与恢复密码封装，保留设备锁和其他用户
- [x] 4.6 为创建需验证、导入封装、迁移跳过/失败保留旧数据添加测试

## 5. 共享 UI

- [x] 5.1 实现首次安装设置主密码页（可选生物识别），完成后进入用户列表并打开五分钟会话
- [x] 5.2 用户列表增加设备设置入口；列表本身不验证、不解密 Vault
- [x] 5.3 抽取共用验证弹层：按偏好自动生物识别、可切主密码、可走忘记主密码
- [x] 5.4 创建/导入与点选用户在无会话时先验证；点选待重绑用户改为恢复密码绑定
- [x] 5.5 实现首页设备设置：生物识别开关、修改主密码、忘记主密码（选用户+恢复密码）
- [x] 5.6 会话到期清理解密 Vault 并回到用户列表；有效期内回列表保留 DeviceLEK，可直接开另一用户或创建用户
- [x] 5.7 从创建用户表单和用户解锁页移除每用户主密码字段与并列的生物识别按钮

## 6. 回归与文档注释

- [x] 6.1 为新增/改动的手写类型与函数补中文文档注释，复杂密钥路径补说明约束的行内注释
- [x] 6.2 跑共享单元测试及 Android/Desktop 相关测试，确认备份导出仍不含设备锁材料
